# SuperMath
A simple mathematical calculation package.

## Installation
Use the package manager [pip](https://pip.pypa.io/en/stable/) to install SuperMath.

```bash
pip install .
```

## Usage
```python
from my_package import add, subtract
result = add(1, 2)
print(result)  # 3
```
